<Window x:Class="ZapretUI.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mvvm="clr-namespace:ZapretUI.Mvvm"
        Title="Что нового" Width="520" Height="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}" Foreground="{StaticResource FgBrush}"
        FontFamily="{StaticResource UiFont}">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FgBrush}"/>
            <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Что нового" FontFamily="{StaticResource DisplayFont}"
                       FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock x:Name="VersionText" Style="{StaticResource Muted}" FontSize="13"/>
        </StackPanel>

        <!-- Changelog content -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ChangelogText" TextWrapping="Wrap"
                           FontSize="13.5" LineHeight="22"/>
            </ScrollViewer>
        </Border>

        <!-- Close button -->
        <Button Grid.Row="4" Content="Понятно" Click="OnClose"
                Style="{StaticResource PrimaryButton}" Width="120" Height="36"
                HorizontalAlignment="Right"/>
    </Grid>
</Window>
